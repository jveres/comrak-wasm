{
	"name": "comrak-wasm",
	"version": "0.0.0",
	"description": "WebAssembly bindings for comrak — a fast CommonMark + GFM Markdown parser and renderer",
	"license": "MIT",
	"private": true,
	"type": "module",
	"main": "pkg/comrak.js",
	"types": "types.d.ts",
	"repository": {
		"type": "git",
		"url": "https://github.com/jveres/comrak-wasm"
	},
	"homepage": "https://github.com/jveres/comrak-wasm",
	"scripts": {
		"build": "cargo build --target wasm32-unknown-unknown --release && wasm-bindgen --target web --out-name comrak --out-dir pkg target/wasm32-unknown-unknown/release/comrak_wasm.wasm && mv pkg/comrak_bg.wasm pkg/comrak.wasm && mv pkg/comrak_bg.wasm.d.ts pkg/comrak.wasm.d.ts && perl -i -pe 's/comrak_bg\\.wasm/comrak.wasm/g' pkg/comrak.js pkg/comrak.d.ts pkg/comrak.wasm.d.ts",
		"check": "biome check .",
		"check:fix": "biome check --write .",
		"typecheck": "tsc --noEmit",
		"test": "vitest run",
		"shiki": "cd examples/shiki && npm install && npm start",
		"dev": "vite examples/playground",
		"md": "node examples/md.mjs"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.1",
		"@types/node": "^26.0.1",
		"@vitest/coverage-v8": "^4.1.9",
		"katex": "^0.17.0",
		"shiki": "^4.3.0",
		"typescript": "^6.0.3",
		"vite": "^8.1.2",
		"vitest": "^4.1.9"
	}
}
